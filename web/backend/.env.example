# 이 파일을 복사해 .env로 만들어 사용
# cp .env.example .env

# HDFS 경로 루트 (로컬 테스트: 로컬 경로 / 운영: hdfs://namenode:9000)
HDFS_BASE=/tmp/stock-data

# Mock 모드 (true: mock/*.json 반환 / false: 실제 HDFS 읽기)
USE_MOCK=true

# CORS 허용 오리진 (콤마 구분)
CORS_ORIGINS=http://localhost:3000
